version: 1
frontend:
  phases:
    preBuild:
      commands:
        - "cd cv_next"
        - "npm ci"
    build:
      commands:
        - "npm run build"
  artifacts:
    baseDirectory: /cv_next/.next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - "cd cv_next"
        - "echo UPDATED"
        - "npm ci"
    build:
      commands:
        - "npm run build"
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*

version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - "cd cv_next"
            - "echo UPDATED"
            - "npm ci"
        build:
          commands:
            - "npm run build"
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
